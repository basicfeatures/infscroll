{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@hotwired/turbo-rails": "^7.1.3",
    "esbuild": "^0.15.5",
    "esbuild-rails": "^1.0.3"
  },
  "scripts": {
    "build": "node esbuild.config.js",
    "build:default": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/asses/builds --public-path=assets"
  }
}
